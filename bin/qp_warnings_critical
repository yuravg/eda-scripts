#!/usr/bin/env bash

# Show only critical compilation warnings Quartus Prime

CMD=grep
if hash rg 2>/dev/null; then
    CMD=rg
fi

qp_warnings_map | "$CMD" -e Critical
qp_warnings_sta | "$CMD" -e Critical
qp_warnings_fit | "$CMD" -e Critical
